name: new-api
description: Create a new FastAPI endpoint
steps:
  - Ask for endpoint name and purpose
  - Create FastAPI route with proper error handling
  - Add health check endpoint
  - Create test script
  - Add to start_all_apis.py if needed
